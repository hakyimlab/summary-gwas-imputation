# -----------------------------------------------------------------------------
# SET UP ENVIRONMENT

suppressWarnings(suppressPackageStartupMessages(library(susieR)))

# -----------------------------------------------------------------------------
# SUSIER FUNCTION

run_susier <- function(phenos, geno_matrix){
  # phenos <- vector of length N
  # geno matrix <- matrix of size N x P (N individuals with P variants)
  # I think we need to run susie() and then susie_get_pip()

}